# Phoenix Server
PHX_SERVER=true
PORT=4000

# Database (optional in dev, uses config/dev.exs defaults)
# DATABASE_URL=ecto://user:pass@localhost/lnm3_platform

# Production secrets (required in prod)
# SECRET_KEY_BASE=generate_with_mix_phx.gen.secret
# PHX_HOST=example.com
# CORS_ORIGINS=https://example.com,https://app.example.com

# Optional
# DNS_CLUSTER_QUERY=myapp.internal
# POOL_SIZE=10
# ECTO_IPV6=false
